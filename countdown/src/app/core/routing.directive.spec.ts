import { EmitFromInputDirective } from './emit-from-input.directive';

describe('RoutingDirective', () => {
  it('should create an instance', () => {
    const directive = new EmitFromInputDirective();
    expect(directive).toBeTruthy();
  });
});
